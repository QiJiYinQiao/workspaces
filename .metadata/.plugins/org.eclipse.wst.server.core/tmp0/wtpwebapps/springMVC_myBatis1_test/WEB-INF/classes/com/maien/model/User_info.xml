<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC   
    "-//mybatis.org//DTD Mapper 3.0//EN"  
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
    
<mapper namespace="user_info">
	<resultMap id="user" type="com.maien.model.User_info">
		<id column="id" property="id"/>
		<result column="name" property="name" />
	</resultMap>
	<select id="selectUser_info" parameterType="int" resultMap="user">
		select * from user_info
	</select>
	
	<!-- <select id="selectUser_infoById" parameterType="int" resultMap="user">
		select * from user_info where id = #{id}
	</select>
	
	<insert id="insert_user" parameterType="com.maien.model.User_info">
		insert into user_info(name) values(#{name})
	</insert> -->
	
</mapper>